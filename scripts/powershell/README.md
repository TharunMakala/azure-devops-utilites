# PowerShell Scripts

All scripts are compatible with PowerShell 7+ (cross-platform) and Windows PowerShell 5.1.

Run `Get-Help .\<ScriptName>.ps1 -Full` for complete parameter documentation on any script.
